export type Occupation = 'Farmer'|'Fisherman'|'Business Owner'|'Traveler'|'Aviation'|'Marine'|'Urban/City'|'Researcher'|'Disaster Management'|'General Public'|'Other';
export type Language = { code: string; label: string; native: string };
export type LocationData = { id:string; name:string; district?:string; state:string; country:string; lat:number; lon:number; type?:string };
export type WeatherCurrent = { temp: number; feelsLike:number; humidity:number; windSpeed:number; windDir:string; rainProb:number; visibility:number; aqi?:number; condition:string; updatedAt:string; sources:string[]; confidence:'Low'|'Medium'|'High' };
export type HourlyForecast = { time:string; temp:number; icon:string; rainProb?:number };
export type DailyForecast = { day:string; icon:string; rainProb:number; high:number; low:number; condition:string };
export type Risk = { hazard:string; level:'Low'|'Moderate'|'High'|'Critical'; probability?:number; time?:string; location:string; sources:string[]; confidence:string; action:string };
export type Alert = { id:string; type:'normal'|'critical'; title:string; message:string; messageLocal:string; level:string; location:string; time:string; hazard:string; actions?:string[] };
export type CommunityReport = { id:string; text:string; lat:number; lon:number; time:string; status:'Unverified'|'Under Review'|'Corroborated'|'Verified'; user:string; image?:string };
export type UserProfile = { name:string; photo?:string; occupation:Occupation; primaryLang:string; secondaryLang:string; fallbackNote?:string; location:LocationData; alertPrefs:any; voicePrefs:any; accessibility:any; familySafety?:any; notificationPrefs?:any };
